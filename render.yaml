services:
  - type: web
    name: brain-tumor-cnn
    env: python
    runtime: python3.10

    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"

    plan: free

    autoDeploy: true
